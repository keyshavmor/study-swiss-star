flowchart TB
  APICHAT["/api/chat server route<br/>frontend/src/routes/api/chat.ts<br/>CURRENT - FRONTEND"]
  CTXCLIENT["requestContextAnswer()<br/>lib/context-backend.server.ts<br/>CURRENT - FRONTEND"]

  subgraph LOCALBACKEND["Local machine :8001 - contract boundary only"]
    ENDPOINT["POST /api/chat<br/>EXPECTED BACKEND CONTRACT"]
    CTXMGR["Context manager<br/>BACKEND IMPLEMENTATION UNKNOWN"]
    RAG["Retrieval / RAG<br/>BACKEND IMPLEMENTATION UNKNOWN"]
    DOCPROC["Document processing<br/>BACKEND IMPLEMENTATION UNKNOWN"]
    MEMORY["Student memory<br/>BACKEND IMPLEMENTATION UNKNOWN"]
    ORCH["AI orchestration<br/>BACKEND IMPLEMENTATION UNKNOWN"]
    MODELIF["Local model interface<br/>BACKEND IMPLEMENTATION UNKNOWN"]
    MEDIAPROC["Media processing<br/>BACKEND IMPLEMENTATION UNKNOWN"]
    WEBRET["Web retrieval<br/>BACKEND IMPLEMENTATION UNKNOWN"]
  end

  MODEL["Local model runtime :8000<br/>llama.cpp / Qwen<br/>BACKEND IMPLEMENTATION UNKNOWN"]

  APICHAT --> CTXCLIENT
  CTXCLIENT -->|"X-Student-Id header, JSON body"| ENDPOINT
  ENDPOINT --> CTXMGR
  CTXMGR --> RAG
  CTXMGR --> DOCPROC
  CTXMGR --> MEMORY
  CTXMGR --> ORCH
  ORCH --> MODELIF
  MODELIF --> MODEL
  CTXMGR --> MEDIAPROC
  CTXMGR --> WEBRET
  ENDPOINT -->|"ContextChatResponse or error envelope"| CTXCLIENT


  subgraph LOCALBACKEND2["Local machine :8001 - admission/safety/messaging contract surfaces (this pass)"]
    ADMISSIONCHECK["POST /api/system/admission/check<br/>EXPECTED BACKEND CONTRACT"]
    HEALTHEP["GET /api/system/health<br/>EXPECTED BACKEND CONTRACT"]
    HEARTBEATEP["POST /api/system/session/heartbeat<br/>EXPECTED BACKEND CONTRACT"]
    RELEASEEP["POST /api/system/runtime/release<br/>EXPECTED BACKEND CONTRACT"]
    RECOMMENDEP["GET /api/system/model/recommendation<br/>EXPECTED BACKEND CONTRACT"]
    MODERATEEP["POST /api/safety/moderate<br/>surfaces: ai_prompt, peer_message<br/>EXPECTED BACKEND CONTRACT"]
    PEERSENDEP2["POST /api/peer-messaging/send<br/>EXPECTED BACKEND CONTRACT"]
    ATTACHSCANEP["POST /api/safety/attachment-scan<br/>EXPECTED BACKEND CONTRACT"]
  end

  CTXCLIENT -->|"admission lease check, fail-closed"| ADMISSIONCHECK
  CTXCLIENT -->|"unavailable -> SystemHealthPanel shows unavailable"| HEALTHEP
  CTXCLIENT --> HEARTBEATEP
  CTXCLIENT --> RELEASEEP
  CTXCLIENT --> RECOMMENDEP
  APICHAT -->|"moderate ai_prompt before store/answer, 503 safety_unavailable when missing"| MODERATEEP
  CTXCLIENT -->|"fail-closed send when missing"| PEERSENDEP2
  CTXCLIENT -->|"no attachment scan available when missing"| ATTACHSCANEP

%% CURRENT (2026-09-17): the authenticated startup flow is auth → PER-SESSION
%% language decision (select or explicit skip) → PER-SESSION model decision
%% (backend-confirmed ready, or explicit continue-without-AI) → product. Language
%% onboarding is NOT once-per-account; language_onboarding_completed is legacy
%% metadata only. There is NO mandatory system-admission screen between them, and
%% /onboarding/model redirects to /onboarding/language while that decision is
%% missing. Canonical: docs/sequences/POST_LOGIN_STARTUP.mmd. Model/GPU readiness
%% lives in sessionStorage only, never in Supabase. auto_storage_cleanup user
%% preference removed; cleanup is a platform-wide cron (STORAGE_CAPACITY_CLEANUP.mmd).

%% Compliance onboarding, suspension, legal document routes, peer messaging and the
%% system health panel: see docs/sequences/PEER_CHAT_CREATION_BY_USERNAME.mmd,
%% docs/sequences/SYSTEM_HEALTH_AGGREGATION.mmd and docs/contracts/BACKEND_GAP_MATRIX.md.
%% The system-admission screen is OPTIONAL and no longer part of the startup order
%% (capability/admission/recommendation data is rendered on the model screen).
