{
  "dependencies": {},
  "devDependencies": {
    "drizzle-kit": "^0.31.10",
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.9"
  },
  "scripts": {
    "frontend:install": "cd frontend && bun install",
    "dev": "bun run frontend:install && cd frontend && bun run dev",
    "build": "bun run frontend:install && cd frontend && bun run build",
    "build:dev": "bun run frontend:install && cd frontend && bun run build:dev",
    "preview": "cd frontend && bun run preview",
    "typecheck": "bun run frontend:install && cd frontend && bun run typecheck"
  }
}
